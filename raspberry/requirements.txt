# ==============================================
# AI Art Gallery - Raspberry Pi Dependencies
# ==============================================

# Environment variables
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# Web server for streaming (MJPEG)
aiohttp>=3.9.0

# WebSocket client (EC2 스트림 푸시용)
websockets>=12.0

# Image processing
numpy>=1.24.0
Pillow>=10.0.0

# MediaPipe for advanced person detection (optional but recommended)
# Provides deep learning based detection with higher accuracy
# Falls back to HOG (OpenCV) if not installed
mediapipe>=0.10.0

# ==============================================
# System packages (install via apt)
# ==============================================
# OpenCV, Picamera2, and GPIO libraries should be installed via apt
# because pip build takes too long (30+ minutes)
#
# sudo apt install -y python3-opencv python3-picamera2 python3-gpiozero
#
# Use --system-site-packages when creating venv:
# python3 -m venv venv --system-site-packages

# ==============================================
# Installation steps (Raspberry Pi)
# ==============================================
# 1. Install system packages
#    sudo apt update
#    sudo apt install -y python3-opencv python3-picamera2
#
# 2. Create venv with system packages
#    python3 -m venv venv --system-site-packages
#    source venv/bin/activate
#
# 3. Install pip packages
#    pip install -r requirements.txt
