# ==============================================
# AI Art Gallery - 라즈베리파이 의존성
# ==============================================

# 환경변수 관리
python-dotenv>=1.0.0

# HTTP 요청
requests>=2.31.0

# 이미지 처리
numpy>=1.24.0
Pillow>=10.0.0

# ==============================================
# 시스템 패키지로 설치 (apt 사용)
# ==============================================
# OpenCV와 Picamera2는 pip 빌드가 오래 걸리므로
# apt로 설치하는 것을 권장합니다.
#
# sudo apt install -y python3-opencv python3-picamera2
#
# 가상환경에서 시스템 패키지 사용:
# python3 -m venv venv --system-site-packages

# ==============================================
# 전체 설치 방법 (라즈베리파이)
# ==============================================
# 1. 시스템 패키지 설치
#    sudo apt update
#    sudo apt install -y python3-opencv python3-picamera2
#
# 2. 가상환경 생성 (시스템 패키지 포함)
#    python3 -m venv venv --system-site-packages
#    source venv/bin/activate
#
# 3. pip 패키지 설치
#    pip install -r requirements.txt
